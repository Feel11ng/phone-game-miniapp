{
  "name": "phone-game-bot",
  "version": "1.0.0",
  "description": "Phone Game Telegram Bot with Mini App",
  "scripts": {
    "start": "serve public -s -l $PORT",
    "vercel-build": "echo 'Build step not needed'"
  },
  "dependencies": {
    "serve": "^14.2.0"
  },
  "engines": {
    "node": ">=14.x"
  }
}
